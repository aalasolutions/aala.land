# Frontend Technical Memories

20260708||ARCHITECTURE||Template dedup audit + extraction plan delivered (branch FE-Rearrange). 5 parallel agents audited 24 templates (~14k lines). Registry: .claude/memory/dedup-registry.md. Executable plan: docs/DEDUPLICATION_PLAN.md. Highest-leverage items: unified Ui::Badge (12 files), Ui::PageHeader (10), Ui::EmptyState (9), Ui::ModalFooter (12), Ui::StatTile, Ui::DataTable+single-pagination, Lead::LeadCard (~210 dup lines in leads.hbs). App module prefix is `land` (e.g. import from 'land/constants'). All CSS in app/styles/app.scss (partials _variables, _nuvoui). Components are classic .hbs+.js Glimmer; Ui::FormInput fires onChange with raw DOM event. ~21 raw <input> blocks bypass Ui::FormInput (top reuse fix). Aamir executes batches himself; no app code changed by audit.

20260709||ARCHITECTURE||Dedup Batch 1 EXECUTED (UNCOMMITTED): F2 audit formatDate->format-date-time (added withSeconds named opt, helper convention matches format-date); F3 9 raw dates->format-date across leases/cheques x2/financials x2/commissions/maintenance x2; F4 audit svg chevron->Ui::Ph (kept CSS rotation per Aamir, class merges via ...attributes); F1 17 raw inputs->Ui::FormInput Groups A+B (accept-invite x2, cheques x2, properties detail x7, unit x6). Two-column fields wrapped in flex-1 div (matches existing cheques-amount pattern). All {{on "input"}}->@onChange; data-test-* preserved via splat. SKIPPED: profile Group C (form-group/form-label layout), file inputs, textareas. Gotcha: Ui::FormInput renders own label+wrapper, hardcodes {{on "input"}}, ...attributes splats onto inner input (class merges).

20260709||ARCHITECTURE||Enum/taxonomy reconciliation discovered (CROSS-REPO, BLOCKING). FE constants.js overhauled but 12 backend Postgres enums still minimal; 12/19 option arrays diverge, 8 have renamed keys needing data rewrite (CREDIT_CARD->CREDIT_DEBIT_CARD, OPEN->OPEN_NEW, CLEANING->CLEANING_DEEP, ID_COPY->ID_PASSPORT, VIEWING/DEAD/COMMERCIAL split). Plan: backend/docs/ENUM_RECONCILIATION_PLAN.md. Recommended Option B (varchar + shared FE/BE constant). BLOCKER before LeadStatus change: leads.service.ts:318 WON-convert + reports.service.ts:378,496 funnel keyed to enum VALUES -> refactor to category-based. Precedent: users_role_enum renamed via _new/_old type recreation. Backend enum type naming: <table>_<column>_enum.
