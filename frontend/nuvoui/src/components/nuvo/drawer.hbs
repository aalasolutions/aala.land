<div
  class={{this.backdropClasses}}
  data-test-nu-drawer-backdrop
  {{dialog-transition
    @open
    onOpen=this.onOpen
    onVisible=this.onVisible
    onHide=this.onHide
    onHidden=this.onHidden
  }}
>
  {{#if (and this.isMounted this.closeOnBackdrop)}}
    {{! A button, not a click handler: only interactive elements take clicks. }}
    <button
      type="button"
      class="nu-dialog-dismiss"
      tabindex="-1"
      aria-hidden="true"
      data-test-nu-drawer-backdrop-dismiss
      {{on "click" this.onClose}}
    ></button>
  {{/if}}
  {{#if this.isMounted}}
    <div
      class={{this.drawerClasses}}
      role="dialog"
      aria-modal="true"
      aria-labelledby={{if (or (has-block "header") @title) this.titleId}}
      tabindex="-1"
      data-test-nu-drawer
      {{dialog onEscape=this.onClose closeOnEsc=@closeOnEsc}}
      ...attributes
    >
      {{#if (has-block "header")}}
        <div class="nu-drawer__header">
          {{yield this.titleId to="header"}}
        </div>
      {{else if @title}}
        <div class="nu-drawer__header">
          <h2 id={{this.titleId}} class="nu-drawer__title" data-test-nu-drawer-title>
            {{@title}}
          </h2>
          <button
            type="button"
            class="nu-drawer__close"
            aria-label="Close"
            data-test-nu-drawer-close
            {{on "click" this.onClose}}
          >
            ✕
          </button>
        </div>
      {{/if}}

      <div class="nu-drawer__body" data-test-nu-drawer-body>
        {{#if (has-block "body")}}
          {{yield to="body"}}
        {{else}}
          {{yield}}
        {{/if}}
      </div>

      {{#if (has-block "footer")}}
        <div class="nu-drawer__footer" data-test-nu-drawer-footer>
          {{yield to="footer"}}
        </div>
      {{/if}}
    </div>
  {{/if}}
</div>
