<NuvoDocs::Page
  @id="alert"
  @title="Alert"
  @subtitle="Inline message in six tones, with optional description, actions and dismiss."
  @components="Nuvo::Alert"
>
  <NuvoDocs::Section @title="Overview" @id="overview">
    <p class="nuvo-doc__intro">
      An alert reports a condition in place. The icon slot grows when a
      description is present and stays small for title-only alerts, so the
      layout follows the data you give it. Dismissal is a callback: the
      alert does not hide itself. For transient feedback after an action use
      a toast instead.
    </p>
  </NuvoDocs::Section>

  <NuvoDocs::Section @title="Features" @id="features">
    <ul class="nu-list">
      <li class="nu-list__item">Six colour variants; title, description or custom block content.</li>
      <li class="nu-list__item">Icon size follows the presence of a description.</li>
      <li class="nu-list__item">Inline and banner layouts.</li>
      <li class="nu-list__item">Closable with a labelled dismiss button; actions slot.</li>
    </ul>
  </NuvoDocs::Section>

  <NuvoDocs::Section @title="Variants" @id="variants">
    {{#each this.variantExamples as |example|}}
      <NuvoDocs::Example @title={{example.title}} @note={{example.note}} @layout="block" @code={{example.code}}>
        <Nuvo::Alert @variant={{example.variant}} @icon="ℹ" @title="{{example.title}} alert" />
      </NuvoDocs::Example>
    {{/each}}
  </NuvoDocs::Section>

  <NuvoDocs::Section @title="Content" @id="content">
    <NuvoDocs::Example
      @title="Title and description"
      @note="The description adds a second line and the icon grows to match. For anything the user needs a sentence to understand."
      @layout="block"
      @code={{this.code.description}}
    >
      <Nuvo::Alert
        @variant="success"
        @icon="✓"
        @title="Rent collected"
        @description="AED 42,000 was collected across 6 units this week."
      />
    </NuvoDocs::Example>

    <NuvoDocs::Example
      @title="Icon suppressed"
      @note="showIcon false hides the glyph while the data keeps it, for a stack of alerts where icons would repeat."
      @layout="block"
      @code={{this.code.noIcon}}
    >
      <Nuvo::Alert
        @variant="info"
        @icon="ℹ"
        @showIcon={{false}}
        @title="Icon suppressed"
        @description="showIcon false keeps the data but hides the glyph."
      />
    </NuvoDocs::Example>

    <NuvoDocs::Example
      @title="Block content"
      @note="A block replaces title and description for markup such as links and emphasis."
      @layout="block"
      @code={{this.code.block}}
    >
      <Nuvo::Alert @variant="warning" @icon="⚠">
        <strong>Custom body.</strong> Block content replaces title and description.
      </Nuvo::Alert>
    </NuvoDocs::Example>

    <NuvoDocs::Example
      @title="Actions"
      @note="The actions slot puts the next step inside the alert, so the user does not have to hunt for it."
      @layout="block"
      @code={{this.code.actions}}
    >
      <Nuvo::Alert
        @variant="warning"
        @icon="⚠"
        @title="Cheque bounced"
        @description="Post-dated cheque #4821 was returned by the bank."
      >
        <:actions>
          <Nuvo::Button @variant="secondary" @size="sm" @text="Contact tenant" />
          <Nuvo::Button @variant="ghost" @size="sm" @text="Dismiss" />
        </:actions>
      </Nuvo::Alert>
    </NuvoDocs::Example>
  </NuvoDocs::Section>

  <NuvoDocs::Section @title="Layouts" @id="layouts">
    <NuvoDocs::Example
      @title="Inline"
      @note="Title and description on one line, for a compact note above a table or inside a form."
      @layout="block"
      @code={{this.code.inline}}
    >
      <Nuvo::Alert
        @variant="info"
        @icon="ℹ"
        @inline={{true}}
        @title="Inline alert"
        @description="stays on one line"
      />
    </NuvoDocs::Example>

    <NuvoDocs::Example
      @title="Banner"
      @note="Edge to edge without radius, for a page-level notice at the top of the content area."
      @layout="block"
      @code={{this.code.banner}}
    >
      <Nuvo::Alert @variant="primary" @icon="🏠" @banner={{true}} @title="Banner alert spans full width" />
    </NuvoDocs::Example>
  </NuvoDocs::Section>

  <NuvoDocs::Section @title="Dismissal" @id="dismissal">
    <NuvoDocs::Example
      @title="Closable"
      @note="A labelled dismiss button reports through onClose; render the alert conditionally so it is removed rather than hidden."
      @layout="block"
      @code={{this.code.closable}}
    >
      {{#if this.alertDismissed}}
        <div class="nuvo-row">
          <span class="nu-text m-sm m-muted">Danger alert dismissed.</span>
          <Nuvo::Button
            @variant="link"
            @size="sm"
            @text="Reset"
            @onClick={{this.resetAlert}}
            data-test-nuvo-alert-reset
          />
        </div>
      {{else}}
        <Nuvo::Alert
          @variant="danger"
          @icon="⛔"
          @title="Maintenance overdue"
          @description="Unit 118, JVC Residence has an unresolved AC repair ticket open for 9 days."
          @closable={{true}}
          @onClose={{this.dismissAlert}}
        />
      {{/if}}
    </NuvoDocs::Example>
  </NuvoDocs::Section>

  <NuvoDocs::Section @title="Arguments" @id="arguments">
    <NuvoDocs::Table @kind="args" @rows={{this.argRows}} />
  </NuvoDocs::Section>

  <NuvoDocs::Section @title="Callbacks" @id="callbacks">
    <NuvoDocs::Table @kind="callbacks" @rows={{this.callbackRows}} />
  </NuvoDocs::Section>

  <NuvoDocs::Section @title="Blocks" @id="blocks">
    <NuvoDocs::Table @kind="blocks" @rows={{this.blockRows}} />
  </NuvoDocs::Section>

  <NuvoDocs::Section @title="Accessibility" @id="accessibility">
    <ul class="nu-list">
      <li class="nu-list__item">Renders role="alert", so content is announced when it appears. Render alerts conditionally rather than toggling visibility.</li>
      <li class="nu-list__item">The icon is aria-hidden; the dismiss button is labelled "Dismiss alert".</li>
    </ul>
  </NuvoDocs::Section>

  <NuvoDocs::Section @title="RTL" @id="rtl">
    <ul class="nu-list">
      <li class="nu-list__item">Icon, content and dismiss button are laid out with logical gaps and paddings, so the icon leads and the dismiss button trails in both directions.</li>
    </ul>
  </NuvoDocs::Section>
</NuvoDocs::Page>
