<NuvoDocs::Page
  @id="breadcrumb"
  @title="Breadcrumb"
  @subtitle="Navigable trail to the current page."
  @components="Nuvo::Breadcrumb"
>
  <NuvoDocs::Section @title="Overview" @id="overview">
    <p class="nuvo-doc__intro">
      The last item is always the current page and renders as plain text
      with
      <code class="nu-code">aria-current="page"</code>. Earlier items render
      as anchors when they carry an href, or as buttons when they carry an
      onClick. Put it in the page header's breadcrumb slot on detail pages.
    </p>
  </NuvoDocs::Section>

  <NuvoDocs::Section @title="Features" @id="features">
    <ul class="nu-list">
      <li class="nu-list__item">Anchor or button items from one items array.</li>
      <li class="nu-list__item">Current page derived from position, not a flag.</li>
      <li class="nu-list__item">Compact style.</li>
    </ul>
  </NuvoDocs::Section>

  <NuvoDocs::Section @title="Items" @id="items">
    <NuvoDocs::Example
      @title="Links"
      @note="Items with href render as anchors, so the trail works with the browser: hover shows the target, middle-click opens a tab."
      @layout="block"
      @code={{this.code.basic}}
    >
      <Nuvo::Breadcrumb @items={{this.breadcrumbItems}} />
    </NuvoDocs::Example>

    <NuvoDocs::Example
      @title="Click handlers"
      @note="Items with onClick render as buttons, for trails that drive in-app state such as a folder tree or a wizard."
      @layout="block"
      @code={{this.code.click}}
    >
      <Nuvo::Breadcrumb @items={{this.clickItems}} />
      <span class="nu-text m-sm m-muted" data-test-nuvo-crumb-clicked>Last clicked: {{this.lastCrumb}}</span>
    </NuvoDocs::Example>

    <NuvoDocs::Example
      @title="Single item"
      @note="One item is the current page alone; render it so the header layout stays stable on top-level pages."
      @layout="block"
      @code={{this.code.single}}
    >
      <Nuvo::Breadcrumb @items={{this.singleItem}} />
    </NuvoDocs::Example>
  </NuvoDocs::Section>

  <NuvoDocs::Section @title="Styles" @id="styles">
    <NuvoDocs::Example
      @title="Compact"
      @note="Smaller type and tighter separators, the form used inside a page header."
      @layout="block"
      @code={{this.code.compact}}
    >
      <Nuvo::Breadcrumb @items={{this.breadcrumbItems}} @compact={{true}} />
    </NuvoDocs::Example>
  </NuvoDocs::Section>

  <NuvoDocs::Section @title="Arguments" @id="arguments">
    <NuvoDocs::Table @kind="args" @rows={{this.argRows}} />
    <h3 class="nuvo-subtitle">Item shape</h3>
    <NuvoDocs::Table @kind="args" @rows={{this.itemRows}} />
  </NuvoDocs::Section>

  <NuvoDocs::Section @title="Accessibility" @id="accessibility">
    <ul class="nu-list">
      <li class="nu-list__item">Renders a nav labelled "Breadcrumb" wrapping an ordered list.</li>
      <li class="nu-list__item">The current item carries aria-current="page"; separators are drawn by CSS and not read aloud.</li>
    </ul>
  </NuvoDocs::Section>

  <NuvoDocs::Section @title="RTL" @id="rtl">
    <ul class="nu-list">
      <li class="nu-list__item">Separators and item spacing are inline-logical, so the trail reads right to left under dir="rtl".</li>
    </ul>
  </NuvoDocs::Section>
</NuvoDocs::Page>
