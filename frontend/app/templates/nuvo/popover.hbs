<NuvoDocs::Page
  @id="popover"
  @title="Popover"
  @subtitle="Click-triggered panel with an arrow, hosted on the anchored layer."
  @components="Nuvo::Popover"
>
  <NuvoDocs::Section @title="Overview" @id="overview">
    <p class="nuvo-doc__intro">
      A popover holds rich content next to its trigger. Like the dropdown
      menu it renders on the layer host while open, so it escapes clipping
      ancestors, and the anchor modifier positions it, flips it and draws
      the arrow at the trigger's centre. Use it for a small form or a
      confirmation that does not deserve a modal; use a tooltip for text
      alone.
    </p>
  </NuvoDocs::Section>

  <NuvoDocs::Section @title="Features" @id="features">
    <ul class="nu-list">
      <li class="nu-list__item">Four logical placements with automatic flip and viewport clamp.</li>
      <li class="nu-list__item">Optional title with a close button; body and footer slots.</li>
      <li class="nu-list__item">Closes on Escape, outside click, outside scroll and resize; focus returns to the trigger.</li>
      <li class="nu-list__item">Controlled and uncontrolled modes.</li>
    </ul>
  </NuvoDocs::Section>

  <NuvoDocs::Section @title="Modes" @id="modes">
    <NuvoDocs::Example
      @title="Uncontrolled"
      @note="The popover keeps its own open state; the trigger block yields toggle and isOpen. The common case."
      @code={{this.code.uncontrolled}}
    >
      <Nuvo::Popover @title="Details">
        <:trigger as |toggle isOpen|>
          <Nuvo::Button
            @text="Open"
            @variant="secondary"
            @onClick={{toggle}}
            aria-expanded={{if isOpen "true" "false"}}
            data-test-nuvo-popover-uncontrolled
          />
        </:trigger>
        <:body>
          <Nuvo::Text @size="sm" @text="The popover keeps its own open state." />
        </:body>
      </Nuvo::Popover>
    </NuvoDocs::Example>

    <NuvoDocs::Example
      @title="Controlled"
      @note="The caller owns the open flag: the trigger reports through onToggle and every close path reports through onClose. For popovers a page must open from elsewhere."
      @code={{this.code.controlled}}
    >
      <Nuvo::Popover
        @title="Quick actions"
        @open={{this.popoverOpen}}
        @onToggle={{this.setPopoverOpen}}
        @onClose={{this.closePopover}}
      >
        <:trigger as |toggle|>
          <Nuvo::Button
            @text="Unit actions"
            @variant="secondary"
            @onClick={{toggle}}
            data-test-open-popover
          />
        </:trigger>
        <:body>
          <Nuvo::Text @text="Edit, duplicate, or archive this unit." />
        </:body>
        <:footer>
          <Nuvo::Button @text="Close" @size="sm" @variant="ghost" @onClick={{this.closePopover}} />
        </:footer>
      </Nuvo::Popover>
      <span class="nu-text m-sm m-muted" data-test-nuvo-popover-state>Open: {{this.popoverOpen}}</span>
    </NuvoDocs::Example>
  </NuvoDocs::Section>

  <NuvoDocs::Section @title="Placements" @id="placements">
    {{#each this.placementExamples as |example|}}
      <NuvoDocs::Example @title={{example.title}} @note={{example.note}} @code={{example.code}}>
        <Nuvo::Popover @title="Placement" @placement={{example.placement}}>
          <:trigger as |toggle|>
            <Nuvo::Button
              @text={{example.placement}}
              @variant="secondary"
              @onClick={{toggle}}
              data-test-nuvo-popover-placement={{example.placement}}
            />
          </:trigger>
          <:body>
            <Nuvo::Text
              @size="sm"
              @text="Positioned by the anchor modifier; flips when there is no room."
            />
          </:body>
        </Nuvo::Popover>
      </NuvoDocs::Example>
    {{/each}}

    <NuvoDocs::Example
      @title="Align end"
      @note="Hangs from the trigger's end edge instead of centring, for a control at the end of a row so the panel stays on screen."
      @code={{this.code.alignEnd}}
    >
      <Nuvo::Popover @placement="bottom" @align="end">
        <:trigger as |toggle|>
          <Nuvo::Button
            @icon="dots-three"
            @shape="square"
            @variant="secondary"
            @onClick={{toggle}}
            aria-label="More"
          />
        </:trigger>
        <:body>
          <Nuvo::Text @size="sm" @text="Hangs from the trigger's end edge." />
        </:body>
      </Nuvo::Popover>
    </NuvoDocs::Example>
  </NuvoDocs::Section>

  <NuvoDocs::Section @title="Slots" @id="slots">
    <NuvoDocs::Example
      @title="No title"
      @note="Without a title there is no header or close button; Escape and an outside click still close it. For a small panel whose content is self-explanatory."
      @code={{this.code.noTitle}}
    >
      <Nuvo::Popover>
        <:trigger as |toggle|>
          <Nuvo::Button @text="No title" @variant="secondary" @onClick={{toggle}} />
        </:trigger>
        <:body>
          <Nuvo::Text @size="sm" @text="No header or close button. Escape and outside click still close it." />
        </:body>
      </Nuvo::Popover>
    </NuvoDocs::Example>

    <NuvoDocs::Example
      @title="Footer"
      @note="The footer row holds the panel's action, for a lightweight confirmation next to the control that triggers it."
      @code={{this.code.footer}}
    >
      <Nuvo::Popover @title="Confirm">
        <:trigger as |toggle|>
          <Nuvo::Button @text="With footer" @variant="secondary" @onClick={{toggle}} />
        </:trigger>
        <:body>
          <Nuvo::Text @size="sm" @text="Archive this unit?" />
        </:body>
        <:footer>
          <Nuvo::Button @text="Archive" @size="sm" @variant="danger" />
        </:footer>
      </Nuvo::Popover>
    </NuvoDocs::Example>
  </NuvoDocs::Section>

  <NuvoDocs::Section @title="Arguments" @id="arguments">
    <NuvoDocs::Table @kind="args" @rows={{this.argRows}} />
  </NuvoDocs::Section>

  <NuvoDocs::Section @title="Callbacks" @id="callbacks">
    <NuvoDocs::Table @kind="callbacks" @rows={{this.callbackRows}} />
  </NuvoDocs::Section>

  <NuvoDocs::Section @title="Blocks" @id="blocks">
    <NuvoDocs::Table @kind="blocks" @rows={{this.blockRows}} />
  </NuvoDocs::Section>

  <NuvoDocs::Section @title="Accessibility" @id="accessibility">
    <ul class="nu-list">
      <li class="nu-list__item">The panel is role="dialog", labelled by the title when one is given. It does not trap focus; Tab leaves the panel.</li>
      <li class="nu-list__item">Escape closes it and stops propagation, so a modal or drawer underneath stays open. Closing returns focus to the trigger when focus was inside.</li>
      <li class="nu-list__item">Set aria-expanded on the trigger from the yielded isOpen when the trigger is a button.</li>
    </ul>
  </NuvoDocs::Section>

  <NuvoDocs::Section @title="RTL" @id="rtl">
    <ul class="nu-list">
      <li class="nu-list__item">start and end placements resolve against the trigger's computed direction; the panel copies the trigger's dir.</li>
      <li class="nu-list__item">The arrow is positioned from the anchor's centre with physical borders under data-placement, and the stylesheet flips its transform sign under dir="rtl".</li>
    </ul>
  </NuvoDocs::Section>
</NuvoDocs::Page>
