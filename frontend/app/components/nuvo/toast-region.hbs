<div
  class={{this.classes}}
  role="log"
  aria-live="polite"
  data-test-nu-toast-region
  ...attributes
>
  {{! key="id": the getter remaps objects, without it every toast replays its entry animation. }}
  {{#each this.toasts key="id" as |toast|}}
    <div class={{toast.classes}} data-test-toast data-test-toast-type={{toast.type}}>
      <div class="nu-toast__content">
        <p class="nu-toast__title">{{toast.message}}</p>
      </div>
      <button
        type="button"
        class="nu-toast__close"
        aria-label="Dismiss"
        data-test-toast-dismiss
        {{on "click" (fn this.dismiss toast.id)}}
      >
        <Ui::Ph @icon="x" @size="14px" />
      </button>
    </div>
  {{/each}}
</div>
