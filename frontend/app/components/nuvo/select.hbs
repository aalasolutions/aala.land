<select
  class={{this.classes}}
  disabled={{@disabled}}
  data-test-nu-select
  {{on "change" this.handleChange}}
  ...attributes
>
  {{#if @placeholder}}
    <option value="" disabled selected={{unless this.hasSelection true}}>
      {{@placeholder}}
    </option>
  {{/if}}

  {{! Must not be named `option`: Glimmer would shadow the element as a component. }}
  {{#each this.normalizedOptions as |item|}}
    <option
      value={{item.value}}
      selected={{item.selected}}
      data-test-nu-select-option
    >{{item.label}}</option>
  {{/each}}
</select>
