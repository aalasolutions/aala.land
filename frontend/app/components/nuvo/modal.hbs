{{#if @open}}
  <div class={{this.backdropClasses}} data-test-nu-modal-backdrop>
    {{#if this.closeOnBackdrop}}
      {{! A button, not a click handler on the backdrop: only interactive elements take clicks. }}
      <button
        type="button"
        class="nu-dialog-dismiss"
        tabindex="-1"
        aria-hidden="true"
        data-test-nu-modal-backdrop-dismiss
        {{on "click" this.onClose}}
      ></button>
    {{/if}}
    <div
      class={{this.modalClasses}}
      role="dialog"
      aria-modal="true"
      aria-labelledby={{if (or (has-block "header") @title) this.titleId}}
      tabindex="-1"
      data-test-nu-modal
      {{dialog onEscape=this.onClose closeOnEsc=@closeOnEsc}}
      ...attributes
    >
      {{#if (has-block "header")}}
        <div class="nu-modal__header">
          {{yield this.titleId to="header"}}
        </div>
      {{else if @title}}
        <div class="nu-modal__header">
          <div>
            <h2 id={{this.titleId}} class="nu-modal__title" data-test-nu-modal-title>
              {{@title}}
            </h2>
            {{#if @subtitle}}
              <p class="nu-modal__subtitle" data-test-nu-modal-subtitle>{{@subtitle}}</p>
            {{/if}}
          </div>
          {{#if this.showClose}}
            <button
              type="button"
              class="nu-modal__close"
              aria-label="Close"
              data-test-nu-modal-close
              {{on "click" this.onClose}}
            >
              ✕
            </button>
          {{/if}}
        </div>
      {{/if}}

      <div class="nu-modal__body" data-test-nu-modal-body>
        {{#if (has-block "body")}}
          {{yield to="body"}}
        {{else}}
          {{yield}}
        {{/if}}
      </div>

      {{#if (has-block "footer")}}
        <div class="nu-modal__footer" data-test-nu-modal-footer>
          {{yield to="footer"}}
        </div>
      {{/if}}
    </div>
  </div>
{{/if}}
