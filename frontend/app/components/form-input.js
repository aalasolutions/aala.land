import Component from '@glimmer/component';
import { tracked } from '@glimmer/tracking';

export default class FormInputComponent extends Component {
  // No additional logic needed - this is a presentational component
}