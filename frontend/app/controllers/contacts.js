import Controller from '@ember/controller';

// Empty shell; logic lives in contacts/index.js and detail.js, avoids shadowing their state.
export default class ContactsController extends Controller {}
