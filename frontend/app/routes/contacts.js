import AuthenticatedRoute from './authenticated';

// Empty shell; logic lives in contacts/index.js and detail.js, avoids shadowing their config.
export default class ContactsRoute extends AuthenticatedRoute {}
