// Page-size bounds shared by every list endpoint.
export const DEFAULT_PAGE_LIMIT = 20;
export const MAX_PAGE_LIMIT = 500;

// Lists that carry contact personal data (contacts, leads) page at most this many rows.
export const CONTACT_LIST_MAX_LIMIT = 100;
