# Setup Memory

Record verified environment and bootstrap facts here.

## Current

- Backend repo path: `backend/`
- Frontend repo path: `frontend/`
- Control root: `.codex/`

## Log

2026-04-17 | AGENT:@codex | Backend `.env` exists at `backend/.env`.
2026-04-17 | AGENT:@codex | Backend and frontend both already contain `node_modules/`.
2026-04-17 | AGENT:@codex | Current shell did not have `node`, `pnpm`, or `docker` on `PATH` during bootstrap attempts.
